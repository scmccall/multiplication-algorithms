const helper = require('./algorithms-helper');

const javascriptDefault = (a,b) => {
  return a * b
}

const gridMethod = (a,b) => {
  // Multiply each (digit * {tens place})
  // Complexity: n^2
  // Note: involved multi-digit multiplication at each step where
  // there are more than 2 digits in a number

}